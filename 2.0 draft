# v2.0 实时根因分析引擎 — 设计草案

> 日期：2026-06-26

---

## 目标

告警进来 → 自动输出：**什么问题、为什么、还有谁可能接着出事。**

---

## 输入

一批新告警，每条包含：告警类型 + 集群名。比如 20 条。

## 输出

```
🚨 告警风暴：20 条告警，20 个集群

🔍 根因分析：
  #1 HGST HUS728T8TAL W4J0（4 集群共享，历史故障率 49x）
  #2 SAMSUNG MZQL23T8HCLS（4 集群共享，历史故障率 7x）

⚠️ 同风险集群（建议提前巡检）：
  CdsEbsSsdCluster-A-20230605-15eb | 交通银行
  CdsEbsSsdCluster-A-20230903-396b | 浙江大数据局

📋 历史相似案例：
  AYOSS-ZSYD | 舟山市政府（15 种告警重叠）

📌 版本：20/20 集群在 v3.16.2r（高风险版本）
```

---

## 五个维度

### 维度 1：根因硬件（共享磁盘型号）

**做什么：** 告警涉及的集群，它们共享哪些磁盘型号。

**怎么做：**

```python
# 统计每个磁盘型号被多少个告警集群共享
model_counter = Counter()
for cluster in alert_clusters:
    cluster_models = set(disks[disks['cluster_name'] == cluster]['disk_model'])
    for model in cluster_models:
        model_counter[model] += 1

# 按共享度排序，取 Top N
shared_models = sorted(model_counter.items(), key=lambda x: -x[1])
```

**数据来源：** machine_disk_detail.disk_model

---

### 维度 2：历史证据（查 L1 是否有前科）

**做什么：** 共享的磁盘型号，在 L1 离线分析中是否有高风险记录。

**怎么做：**

```python
# 加载 L1 批次分析结果
l1_batch = pd.read_csv('output/step3_l1/l1_batch_confirmed.csv')

# 对每个共享型号，查 L1 记录
for model, count in shared_models:
    evidence = l1_batch[l1_batch['disk_model'].str.contains(model[:20], na=False)]
    if len(evidence) > 0:
        lift = evidence.iloc[0]['lift']
        print(f"  {model}: 历史故障率 {lift:.0f}x")
    else:
        print(f"  {model}: 无历史记录")
```

**数据来源：** L1 批次分析结果（v1.0 产出）

---

### 维度 3：同风险集群（硬件配置相似但还没告警）

**做什么：** 硬件配置和告警集群相似，但现在还没告警的集群——提前预警。

**怎么做：**

```python
# 1. 构建告警集群的硬件画像
alert_profiles = {}
for cluster in alert_clusters:
    alert_profiles[cluster] = set(disks[disks['cluster_name'] == cluster]['disk_model'])

# 2. 构建全量集群的硬件画像（可预计算，定期刷新）
all_profiles = {}  # cluster → set of disk_models

# 3. 算 Jaccard 相似度
risk_scores = {}
for alert_c, alert_models in alert_profiles.items():
    for other_c, other_models in all_profiles.items():
        if other_c in alert_clusters:  # 跳过已经在告警中的
            continue
        jaccard = len(alert_models & other_models) / len(alert_models | other_models)
        if jaccard > 0.5:  # 阈值
            # 加分：共享高风险硬件加权
            shared_risk = alert_models & other_models & high_risk_models
            risk_scores[other_c] = jaccard + len(shared_risk) * 0.5

# 4. 排序输出
top_risk = sorted(risk_scores.items(), key=lambda x: -x[1])[:10]
```

**数据来源：** machine_disk_detail.disk_model（全量集群硬件画像预计算）

---

### 维度 4：告警模式匹配（历史上类似告警组合）

**做什么：** 当前这批告警的类型组合，历史上哪些集群也出现过——参考当时的处理方案。

**怎么做：**

```python
# 1. 当前告警类型集合
alert_set = set(current_alerts['alert_type'])

# 2. 遍历所有集群，算告警类型重叠度
cluster_matches = {}
for cluster, group in alerts.groupby('cluster_name'):
    cluster_alert_set = set(group['alert_type'])
    overlap = len(alert_set & cluster_alert_set)
    if overlap >= 3:  # 至少 3 种告警重叠
        cluster_matches[cluster] = overlap

# 3. 排序输出
top_matches = sorted(cluster_matches.items(), key=lambda x: -x[1])[:10]
```

**数据来源：** unique_storage_alert（全量历史告警记录）

---

### 维度 5：版本风险

**做什么：** 告警集群跑的 Pangu 版本，是否有已知风险。

**怎么做：**

```python
# 查告警集群的版本
alert_versions = clusters[clusters['cluster_name'].isin(alert_clusters)]['product_commit_tag']

# 提取主版本号
def extract_major(tag):
    m = re.match(r'(v\d+\.\d+\.\d+r)', str(tag))
    return m.group(1) if m else None

versions = alert_versions.apply(extract_major)

# 匹配 L1 版本分析结果
if all(v == 'v3.16.2r' for v in versions):
    print("⚠️ 全部在 v3.16.2r（已知高风险版本）")
```

**数据来源：** cds_clusters_unique.product_commit_tag + L1 版本分析结果

---

## 技术架构

```
新告警进来
    │
    ▼
┌─────────────────────────────────────┐
│ 1. 提取告警集群 + 告警类型           │
└─────────────────────────────────────┘
    │
    ├──→ 查磁盘表 → 共享硬件（维度 1）
    ├──→ 查 L1 结果 → 历史证据（维度 2）
    ├──→ 查硬件画像 → 同风险集群（维度 3）
    ├──→ 查历史告警 → 告警模式（维度 4）
    └──→ 查版本表 → 版本风险（维度 5）
    │
    ▼
┌─────────────────────────────────────┐
│ 2. 聚合结果 → 排序 → 生成报告       │
└─────────────────────────────────────┘
```

## 性能考虑

- 维度 1/2/5：秒级（直接查表）
- 维度 3：需要预计算全量集群硬件画像（Jaccard 慢），定期刷新（如每周）
- 维度 4：需要预计算或采样（全量集群遍历慢），可先取 Top 1000 集群

## 与 v1.0 的关系

- v1.0（已完成）：离线分析，产出"哪些批次有问题"的清单
- v2.0（本草案）：实时查询，告警来了秒级定位根因
- v1.0 的 L1 结果是 v2.0 的历史证据库（维度 2）
- v1.0 的版本分析结果是 v2.0 的版本风险库（维度 5）
